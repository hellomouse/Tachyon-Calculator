/* Basic stat functions */

'use strict';

module.exports = {
    min: function(arr) {

    },

    max: function(arr) {
        
    },

    zScore: function(x, mean, sigma) {
        /* @help Return z score for x for given normal dist. */
        return (x - mean) / sigma;
    }
};